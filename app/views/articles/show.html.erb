<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>

<h1 id="title"><%= @article.title %></h1>
<span><%= get_type_text @article.level %></span>

<p>
  <%= raw @article.rendered_content %>
</p>
